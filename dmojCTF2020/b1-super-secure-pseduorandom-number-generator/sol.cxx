#include <bits/stdc++.h>
using namespace std;

int main(int argc, char **argv) {
    
    srand(time(NULL));
    fflush(stdin);
    cout << 1 << endl;
    fflush(stdin);
    cout << 13 << endl;
    fflush(stdin);
    cout << 2 << endl;
    fflush(stdin);
    for (int i = 0; i < 13; ++i) rand();
    cout << rand() << endl;
    fflush(stdin);
    return 0;
}
